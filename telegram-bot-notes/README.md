# 📝 Telegram-бот для заметок

Простой Telegram-бот, позволяющий создавать, просматривать, редактировать и удалять заметки.  
Реализовано хранение данных в **PostgreSQL**, развёртывание через **Docker**, данные сохраняются даже при пересборке образа.

---

## 🚀 Функции бота

- `/start` — приветствие и инструкция  
- `/add <заметка>` — добавить новую заметку  
- `/notes` — показать все заметки  
- `/edit <номер> <текст>` — изменить заметку  
- `/delete <номер>` — удалить заметку  

---

## 🐳 Запуск с помощью Docker

### Требования
- [Git](https://git-scm.com/)
- [Docker Desktop](https://www.docker.com/products/docker-desktop/)

### Инструкция

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/kowa541/notes.git
   cd notes
   

2. Создайте файл .env на основе примера:
      cp .env.example .env
   
   Откройте .env и вставьте свой токен от [@BotFather](https://t.me/BotFather):
      BOT_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ
   DATABASE_URL=postgresql://notes_user:notes_pass@db:5432/notes_db
   

3. Запустите приложение:
      docker compose up --build -d
   

4. Готово! Бот запущен и работает.

---

## 🔁 Проверка сохранения данных (обязательно для защиты)

1. В Telegram: /add Тестовая заметка
2. Выполните в терминале:
      docker compose down
   docker compose up -d
   
3. В Telegram: /notes → заметка сохранена ✅  
4. Пересоберите образ:
      docker compose build
   docker compose up -d
   
5. Снова /notes → заметка всё ещё на месте ✅

> 💡 Данные хранятся в именованном Docker volume postgres_notes_, а не внутри контейнера.

---

## 📁 Структура проекта

notes/
├── bot.py                 # Основной код бота
├── config.py              # Чтение переменных окружения
├── requirements.txt       # Зависимости
├── init.sql               # Инициализация таблицы в PostgreSQL
├── Dockerfile             # Сборка образа бота
├── docker-compose.yml     # Оркестрация: бот + PostgreSQL
├── .env.example           # Пример файла с переменными
└── .gitignore             # Исключает .env и временные файлы
